# 🐛 左パネルカテゴリボタンクリック時の条文選択バグ修正レポート

## 📅 修正日時
2025年10月4日 18:18

## 🔍 バグの詳細

### 問題の現象
- 左パネル「固有表現の種類」のカテゴリボタンをクリックすると、条文選択が常に第1条に戻ってしまう
- 例：第64条を選択している状態で「⏰ 期間表現」ボタンをクリック → 第1条に戻る

### 影響範囲
- すべての章・条文での左パネルカテゴリ選択機能
- ユーザーが任意の条文を選択した状態でのカテゴリ切り替え操作

## 🔧 根本原因の解析

### 1. セッション状態管理の問題
```python
# 問題のあったコード（修正前）
selected_article_idx = st.sidebar.selectbox(
    "条文を選択してください:",
    range(len(article_titles)),
    format_func=lambda x: article_titles[x],
    index=st.session_state.selected_article_idx if st.session_state.selected_article_idx < len(article_titles) else 0
)
```
- 全章で共通の`st.session_state.selected_article_idx`を使用
- 章が切り替わるとインデックスが不整合になる可能性

### 2. st.rerun()による副作用
```python
# 問題のあったコード（修正前）
if st.sidebar.button(...):
    st.session_state.selected_category = category
    st.session_state.category_selection_source = "legend"
    st.rerun()  # ← これが原因でページ全体が再描画
```
- `st.rerun()`により全コンポーネントが再初期化
- selectboxのindex指定が期待通りに動作しない

### 3. selectboxキーの重複
- 複数章で同じキーを使用していた可能性
- Streamlitの内部状態管理で競合が発生

## ✅ 実装した修正内容

### 1. 章ごとの条文選択インデックス管理
```python
# 修正後のコード
# セッション状態のキーでインデックスを管理
if f'chapter_{selected_chapter_idx}_article_idx' not in st.session_state:
    st.session_state[f'chapter_{selected_chapter_idx}_article_idx'] = 0

current_article_idx = st.session_state[f'chapter_{selected_chapter_idx}_article_idx']

# インデックスの範囲チェック
if current_article_idx >= len(article_titles):
    current_article_idx = 0
    st.session_state[f'chapter_{selected_chapter_idx}_article_idx'] = 0
```

**効果**：
- 各章の条文選択状態を独立管理
- 章切り替え時も最後に選択した条文を記憶
- インデックス範囲外エラーを防止

### 2. selectboxにユニークキーを設定
```python
# 修正後のコード
selected_article_idx = st.sidebar.selectbox(
    "条文を選択してください:",
    range(len(article_titles)),
    format_func=lambda x: article_titles[x],
    index=current_article_idx,
    key=f"article_selector_chapter_{selected_chapter_idx}"  # ← ユニークキー
)
```

**効果**：
- 章ごとに独立したselectboxウィジェット
- Streamlit内部状態の競合を回避

### 3. st.rerun()の削除
```python
# 修正後のコード
if st.sidebar.button(...):
    st.session_state.selected_category = category
    st.session_state.category_selection_source = "legend"
    # st.rerun() を削除
```

**効果**：
- セッション状態の更新のみ実行
- 不要なページ再描画を防止
- selectboxの状態保持

## 🧪 修正の検証

### テストスクリプト実行結果
```bash
uv run python test_bug_fix.py

🎉 バグ修正成功: カテゴリ変更後も条文選択が保持されています
✅ selectboxキーは一意です
  - article_selector_chapter_0
  - article_selector_chapter_1  
  - article_selector_chapter_2
```

### 動作確認手順
1. ✅ 任意の章・条文を選択（例：第3章の二　第64条）
2. ✅ 左パネルで「⏰ 期間表現」ボタンをクリック
3. ✅ 条文選択が第64条のまま保持される
4. ✅ ハイライト表示が期間表現に切り替わる

## 📊 修正の技術的影響

### パフォーマンス改善
- `st.rerun()`削除により不要な再描画を削減
- ユーザー操作の応答性向上

### 状態管理の強化
- 章ごとの独立した状態管理により、より直感的な動作
- セッション状態の整合性向上

### コードの保守性
- ユニークキーによる明確なウィジェット識別
- セッション状態の構造化

## 🔄 その他の改善点

### セッション状態初期化の改良
```python
# 章ごとの条文選択インデックスを管理するための初期化
# この方式により、章を切り替えても各章の最後に選択した条文を覚えている
# そして左パネルのカテゴリボタンをクリックしても条文選択が変わらない
```

### 範囲チェック機能追加
- インデックス範囲外アクセスの防止
- データ不整合時の自動復旧

## 🎯 今後の拡張可能性

### 1. 条文履歴機能
- 最近閲覧した条文の記録
- ワンクリックでの履歴アクセス

### 2. ブックマーク機能
- よく参照する条文の保存
- カスタム条文グループの作成

### 3. 検索履歴
- カテゴリ選択履歴の保存
- 検索パターンの学習

## 📝 修正ファイル

### 主要修正ファイル
- `app_with_ner.py` - メインアプリケーション（セッション状態管理、selectbox修正）

### 追加ファイル
- `test_bug_fix.py` - バグ修正検証スクリプト

## 🏁 修正完了の確認

✅ **バグ修正完了**：左パネルカテゴリボタンクリック時に条文選択が保持される  
✅ **動作確認済み**：第64条「一年六月」での期間表現ハイライト切り替え  
✅ **テスト実行完了**：自動テストスクリプトで修正内容を検証  
✅ **アプリ起動確認**：http://localhost:8507 で正常動作

---
**修正者**: GitHub Copilot  
**修正日時**: 2025年10月4日 18:18  
**テスト環境**: uv仮想環境、Streamlit 1.28.0  
**修正タイプ**: セッション状態管理の改善、UIバグ修正
